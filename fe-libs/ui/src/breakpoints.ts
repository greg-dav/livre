// Single phone breakpoint (px), theme-agnostic — shared by the CSS `media` token (built in the theme
// base) and any JS that needs the raw value, so it lives in exactly one place.
export const MOBILE_BREAKPOINT = 640;
