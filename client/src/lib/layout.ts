/**
 * Shared layout constants. `SIDEBAR_PANEL_WIDTH` is the width of every full-length side panel —
 * the Library currently-reading rail and the reading-timeline book list — kept in one place so the
 * app's sidebars stay a consistent width.
 */
export const SIDEBAR_PANEL_WIDTH = 272;
