NODE_ENV=development
PORT=3001
DATA_DIR=./data

# Optional. If unset, a secret is generated and persisted to DATA_DIR/.jwt_secret on first run.
# Set this only to pin a specific secret (e.g. shared across multiple instances). Min 32 chars.
# JWT_SECRET=generate-with-openssl-rand-base64-48
